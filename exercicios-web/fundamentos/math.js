const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); // Math.pow calcula a potência / Math.PI é uma constante que representa o valor de PI

console.log(area);
console.log(typeof Math); // object

