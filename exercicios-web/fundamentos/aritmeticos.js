const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;  // soma normal
const subtracao = d - b; // subtracao normal
const multiplicacao = a * b; // multiplicacao normal
const divisao = d / a; // divisao normal
const modulo = a % 2; // resto da divisao

console.log(soma, subtracao, multiplicacao, divisao, modulo); // 24 10 15 5 1
